import 'react-native-gesture-handler'; // Must be at the top and before any other imports
import { registerRootComponent } from 'expo';
import App from './App';

// Register the main component
registerRootComponent(App);
